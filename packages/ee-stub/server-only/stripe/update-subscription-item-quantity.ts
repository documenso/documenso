/**
 * Stub implementation of updating subscription quantity.
 * In the stub version, does nothing and returns undefined.
 */

export const updateSubscriptionItemQuantity = async ({
  priceId,
  subscriptionId,
  quantity,
}: {
  priceId: string;
  subscriptionId?: string;
  quantity: number;
}) => {
  return undefined;
};
