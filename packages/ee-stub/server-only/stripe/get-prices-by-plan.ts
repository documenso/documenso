/**
 * Stub implementation for getting prices by plan.
 * In the stub version, returns empty arrays.
 */

export const getPricesByPlan = async (plans: string[]) => {
  return {
    community: [],
    enterprise: [],
  };
};
