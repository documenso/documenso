import { PlausibleProvider } from '~/providers/plausible';

import '../styles.css';

export default function App({ Component, pageProps }) {
  return (
    <PlausibleProvider>
      <Component {...pageProps} />;
    </PlausibleProvider>
  );
}
